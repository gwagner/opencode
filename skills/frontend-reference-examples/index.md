# Frontend component references

Read only the component document matching the requested surface.

| Component | Aliases and triggers | Use for |
| --- | --- | --- |
| [Data table](references/data-table.md) | table, results table, history table, paginated table, actionable rows | Consistent titled collections with summaries, semantic columns, empty and status states, optional actionable rows, and pagination. |
| [On-off switch](references/on-off-switch.md) | toggle switch, boolean switch, active/inactive, on/off, paused/running, up/down | A labelled native checkbox with configurable labels for one independent binary state. |

## Adding a reference

Use one lowercase hyphenated Markdown file per component. Follow the component-document contract in [`SKILL.md`](SKILL.md), add one catalog row here, and distinguish illustrative fixtures from authoritative API contracts.
